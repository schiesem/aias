# CQ 02: Which operators are decomposed, and into which sub operators?
PREFIX vdi3682: <https://w3id.org/aias/odp/vdi3682#>
SELECT ?operator ?sub WHERE {
  ?operator vdi3682:isComposedOf ?sub .
} ORDER BY ?operator ?sub
